'''
Given a number, find the next smallest palindrome larger than the number.

Approach A:
Starting from the given number, iterate till you find a palindrome number (i.e. palindrome check on each iteration)

Complexity depends on the number of digits.
Space -> O(N)
Time -> O(sqrt(N))

Approach B:

'''
import pytest
import collections

def next_palin(n):
    pass

def test_next_palin():
    # n = 125
    # assert next_palin(n) == 131
    #
    # n = 11
    # assert even_palin(n) == 22
    pass
